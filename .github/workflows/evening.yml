name: Evening Review (23:00 Taipei, Mon-Sat)

on:
  # 排程交給 cron-job.org（jobId 7516775）經由 API 觸發 workflow_dispatch。
  workflow_dispatch:

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate & push evening review
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_IDS: ${{ secrets.LINE_USER_IDS }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TZ: Asia/Taipei
        run: python scripts/coach.py review

      - name: Commit reviewed marker
        run: |
          git config user.name  "daily-coach-bot"
          git config user.email "bot@users.noreply.github.com"
          git add lessons/
          if ! git diff --cached --quiet; then
            DATE=$(TZ=Asia/Taipei date +%Y-%m-%d)
            git commit -m "lesson: reviewed $DATE"
            git push
          else
            echo "No marker changes to commit."
          fi
